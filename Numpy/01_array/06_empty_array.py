import numpy as np

empty_array = np.empty(5)

# we can create an empty array using the 
# np.empty() function provided by the library.
print("Empty Array:", empty_array)

# It's because although we are creating an empty array, NumPy will try to add some value to it. 
# The values stored in the array are arbitrary and have no significance value.

