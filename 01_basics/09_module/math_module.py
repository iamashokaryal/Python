#A module is a file containing a set of functions
#  that we might want to use in our application. 
# Once we import a module, we can use all the functions defined inside that module.

import math

number = 25

#squre root of number
squre = math.sqrt(number)

print(squre)

# round a number down to the nearest integer
num = 4.8

roundoff = math.floor(num)

print(roundoff)